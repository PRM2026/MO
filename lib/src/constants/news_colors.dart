import 'package:flutter/material.dart';

/// Design tokens for the News screen (Championship theme).
abstract final class NewsColors {
  static const Color gold = Color(0xFFD4AF37);
  static const Color navy = Color(0xFF1A2B48);
  static const Color accent = Color(0xFFEAB308);
  static const Color background = Color(0xFFF9FAFB);
  static const Color surface = Colors.white;
  static const Color textMuted = Color(0xFF6B7280);
  static const Color border = Color(0xFFF3F4F6);
  static const Color featuredBadge = Color(0xFFEF4444);
}
